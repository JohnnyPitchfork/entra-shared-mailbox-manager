<ui:FluentWindow x:Class="SharedMailbox.App.Views.CleanupConfirmDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 mc:Ignorable="d"
                 Title="Confirm removal"
                 Height="520" Width="780"
                 MinHeight="360" MinWidth="560"
                 WindowStartupLocation="CenterOwner"
                 ShowInTaskbar="False"
                 ResizeMode="CanResize">
    <!--
        Modal confirmation shown by ICleanupConfirmationService before any
        Remove-* cmdlet fires. Displays the exact (mailbox, trustee, rights)
        rows about to be revoked. DataContext is a CleanupConfirmDialogViewModel.
        Cancel/Confirm buttons just set DialogResult in code-behind.
    -->
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="{Binding Title}"
                       FontSize="18"
                       FontWeight="SemiBold"/>
            <TextBlock Text="{Binding Subtitle}"
                       TextWrapping="Wrap"
                       Foreground="#666"
                       Margin="0,6,0,0"/>
        </StackPanel>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Items}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  RowHeight="28"
                  AlternatingRowBackground="#F7F7F7">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mailbox"          Binding="{Binding Mailbox}"        Width="2*"/>
                <DataGridTextColumn Header="Trustee"          Binding="{Binding Trustee}"        Width="2*"/>
                <DataGridTextColumn Header="Display name"     Binding="{Binding DisplayName}"    Width="2*"/>
                <DataGridTextColumn Header="Rights to remove" Binding="{Binding RightsToRemove}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <ui:Button Content="Cancel"
                       Click="CancelButton_Click"
                       Width="110" Height="36"
                       Margin="0,0,8,0"/>
            <ui:Button Content="Remove permissions"
                       Click="ConfirmButton_Click"
                       Appearance="Danger"
                       Width="180" Height="36"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
