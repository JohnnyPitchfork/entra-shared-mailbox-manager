{
  "//": "entra-shared-mailbox-manager — bundled default configuration.",
  "//1": "REAL TENANT VALUES MUST NOT BE COMMITTED TO THIS FILE. Either:",
  "//2": "  (a) Create appsettings.local.json next to this file (gitignored) for dev, or",
  "//3": "  (b) Place appsettings.json in %LOCALAPPDATA%\\entra-shared-mailbox-manager\\ for per-user overrides.",
  "//4": "The Config Builder companion app (Pattern A/B/C, see README) generates these.",

  "AzureAd": {
    "TenantId": "00000000-0000-0000-0000-000000000000",
    "ClientId": "00000000-0000-0000-0000-000000000000",
    "GraphScopes": [
      "Group.Read.All",
      "User.Read.All"
    ],
    "ExchangeResource": "https://outlook.office365.com",
    "RedirectUri": "http://localhost"
  },

  "KnownGroups": [
    {
      "//": "Replace with your tenant's SharedMail- security group display name + object ID.",
      "Name": "SharedMail-Example",
      "GroupId": "00000000-0000-0000-0000-000000000000"
    }
  ],

  "//5": "Roles — optional. When empty, every signed-in user sees every KnownGroup (default v1.0 behavior).",
  "//6": "When populated, the sidebar filters to only the SharedMail- groups the user has role-based access to.",
  "//7": "Each role maps an Entra security group of *users* (EntraGroupId) to the SharedMail- groups",
  "//8": "(AllowedGroupIds) its members can administer. AllowedGroupIds should appear in KnownGroups above;",
  "//9": "entries that don't are silently invisible. See docs/Setup.md for the full recipe.",
  "//10": "Example role entry (paste into Roles array in your override, replacing GUIDs with real values):",
  "//11": "  { \"Name\": \"Project Managers\", \"EntraGroupId\": \"<role-group-guid>\", \"AllowedGroupIds\": [\"<sharedmail-group-guid>\"] }",
  "Roles": [],

  "Logging": {
    "//": "Relative paths anchor at %LOCALAPPDATA%\\entra-shared-mailbox-manager\\.",
    "//1": "Absolute paths are used as-is. CSV audit/cleanup/bulk logs and Serilog files share this directory.",
    "LogDirectory": "Logs"
  }
}
